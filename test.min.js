navigator.mediaDevices.getUserMedia({audio:!0}).then(stream=>{const audioCtx=new AudioContext,analyser=audioCtx.createAnalyser();analyser.fftSize=2048,analyser.smoothingTimeConstant=.8;const input=audioCtx.createMediaStreamSource(stream);input.connect(analyser);const javascriptNode=audioCtx.createScriptProcessor(2048,1,1);analyser.connect(javascriptNode),javascriptNode.connect(audioCtx.destination),console.log(audioCtx.sampleRate),javascriptNode.addEventListener("audioprocess",()=>{const frequencyData=new Uint8Array(analyser.frequencyBinCount);analyser.getByteFrequencyData(frequencyData);let values=0;const length=frequencyData.length;for(var i=0;i<length;i++)values+=frequencyData[i];var average=values/length;Math.round(average)>10&&console.log(Math.round(average))})});